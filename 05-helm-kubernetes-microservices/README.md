

This project deploys Google-Microservices demo online shopping application using Kubernetes(AWS EKS cluster + Helm)

1. Create kubernetes cluster
2. install all microservices using helm. Multiple helm releases can be easily installed using helmfile
3. helmfile will create the namespace
3. helmfile apply
4. helmfile apply vs helmfil sync

Exporting metrics
install 